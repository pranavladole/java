//biary search
int result=0;
		for(int i=0;i<arr.length;i++) {
			
			if(arr[i] == k) {
				result=i;
				break;
			}else {
				result=-1;
			}
			
		}
		return result;


//

count no of ele
int count=0;
		
		for(int i=0;i<arr.length;i++) {
			if(arr[i] == target) {
				count+=1;
			}
		}
		
		return count;
